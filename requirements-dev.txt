# Union of every Python service's requirements.txt (backend, agents, etl), for
# running everything natively out of a single virtualenv during local dev.
# Each service also has its own requirements.txt if you'd rather isolate them.

fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
celery==5.4.0
redis==5.0.8
pydantic-settings==2.4.0
PyYAML==6.0.2
neo4j==5.24.0
psycopg[binary]==3.2.1
httpx==0.27.2
requests==2.32.3
beautifulsoup4==4.12.3
